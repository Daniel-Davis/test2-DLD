// Daniel Davis, sum, takes the elements of an array and sums them up //

var array = [3,1,4,1,5,9,2,6,5,4,5,1,3,9,5,7,9,3]; // holds array of numbers
var total = 0; // holds total value

print(total); // displays info
print(array);

for (var i=0, i < array.length, ++i) { // for loop to add them all up
  total = total + [i]; // adds each element to the running total
} // end of for loop

print(array); // displays end result
print(total);
